#include <Arduino.h>

// byte sensorData = 10;
void setup() {

    // Serial.begin(9600);
}

void loop() {
    // Serial.print("Sensor Data is = ");
    // Serial.println(sensorData);
    // sensorData = sensorData + 1;
}